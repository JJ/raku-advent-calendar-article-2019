name: "We 🎔 Perl"
on:
  issues:
    types: [opened, edited, milestoned]

jobs:
  seasonal_greetings:
    runs-on: windows-latest
    steps:
      - name: Merry Xmas!
        env:
          greeting: "Merry"
          season: "Xmas"
          what: ${{github.event}}
        run: |
          $ENV:GREET= $ENV:greeting + " " + $ENV:season + " → " + $ENV:what
          perl -e 'print $ENV{GREET};'
